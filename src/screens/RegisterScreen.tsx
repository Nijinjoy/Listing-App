import { View, Text } from 'react-native'
import React from 'react'
import colors from '../constants/Colors'

const RegisterScreen = () => {
    return (
        <View>
            <Text style={{ color: colors.lightblack }}>RegisterScreen</Text>
        </View>
    )
}

export default RegisterScreen